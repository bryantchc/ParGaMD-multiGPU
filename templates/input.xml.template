<?xml version="1.0" ?>
<gamd>
    <temperature>300</temperature> <!-- unit.kelvin (paper: chignolin runs at 300 K) -->

    <system>
        <nonbonded-method>PME</nonbonded-method>
        <nonbonded-cutoff>0.9</nonbonded-cutoff> <!-- unit.nanometers -->
        <constraints>HBonds</constraints>
    </system>

    <barostat>
        <pressure>1.0</pressure> <!-- unit.bar -->
        <frequency>25</frequency>
    </barostat>
    
    <run-minimization>True</run-minimization>

    <integrator>
        <algorithm>langevin</algorithm>
        <boost-type>upper-dual</boost-type>
        <sigma0>
            <primary>6.0</primary> <!-- unit.kilocalories_per_mole (paper: σ0_P = 6.0 kcal/mol) -->
            <secondary>6.0</secondary> <!-- unit.kilocalories_per_mole (paper: σ0_D = 6.0 kcal/mol) -->
        </sigma0>
	<random-seed>0</random-seed>  <!-- 0 = OpenMM auto-seeds each Context (each segment gets a unique seed). 10 was upstream's testing override. -->

        <dt>0.002</dt> <!-- unit.picoseconds -->
        <friction-coefficient>1.0</friction-coefficient> <!-- unit.picoseconds**-1 -->
        <number-of-steps>
            <conventional-md-prep>0</conventional-md-prep>
	    <conventional-md>0</conventional-md>
            <gamd-equilibration-prep>0</gamd-equilibration-prep>
            <gamd-equilibration>0</gamd-equilibration>
            <gamd-production>0</gamd-production>
            <extension-steps>50000</extension-steps>
            <averaging-window-interval>50000</averaging-window-interval>
        </number-of-steps>

    </integrator>
    
    <input-files>
        <!--
            Generic in-segment-dir names. runseg.sh symlinks the actual
            system-specific files (common_files/${SYSTEM_NAME}.parm7 / .rst7)
            to these names before invoking gamdRunner, so this XML is
            system-agnostic — to swap systems, change SYSTEM_NAME in env.sh,
            don't touch this file.
        -->
        <amber>
            <topology>topology.parm7</topology>
            <coordinates type="rst7">coordinates.rst7</coordinates>
        </amber>
    </input-files>
    <outputs>
        <directory>./out27</directory>
        <overwrite-output>True</overwrite-output>
        <reporting>
            <energy>
                <interval>500</interval>
            </energy>
            <coordinates>
                <file-type>dcd</file-type>
            </coordinates>
            <statistics>
                <interval>500</interval>
            </statistics>
        </reporting>
    </outputs>
</gamd>
